import React, { useState } from "react";

// Single-file React component. Drop into any Create React App / Vite + React app. // Tailwind CSS is used for styling. Make sure Tailwind is configured in your project.

export default function FakeTikTokShopHiran() { const products = [ { id: 1, title: "A Hiran", img: "https://i.postimg.cc/3ky0kmZ8/1.jpg", price: 89.0, msrp: 190.0, tag: "1st order ₱10.00 off", }, { id: 2, title: "Happy Hiran", img: "https://i.postimg.cc/pm95mzBj/2.jpg", price: 89.0, msrp: 190.0, tag: "1st order ₱10.00 off", }, { id: 3, title: "Angry Hiran", img: "https://i.postimg.cc/XrZBrdgC/3.jpg", price: 89.0, msrp: 190.0, tag: "1st order ₱10.00 off", }, ];

const [activeTab, setActiveTab] = useState("Beauty"); const [query, setQuery] = useState(""); const [selected, setSelected] = useState(null);

const filtered = products.filter((p) => p.title.toLowerCase().includes(query.toLowerCase()) );

return ( <div className="min-h-screen bg-white flex items-center justify-center p-4"> <div className="w-[390px] h-[844px] border rounded-2xl overflow-hidden shadow-lg"> {/* Top */} <div className="bg-white sticky top-0 z-10"> <div className="flex items-center justify-between px-4 pt-4"> <div className="text-xs text-gray-400">9:41</div> <div className="flex items-center gap-3"> <div className="text-sm font-semibold">Following</div> <div className="relative"> <div className="absolute -top-3 left-1/2 transform -translate-x-1/2 bg-white px-4 py-2 rounded-full shadow-sm text-sm font-bold">Shop</div> </div> <div className="text-sm text-gray-400">For You</div> <div className="ml-2"> <svg width="20" height="20" viewBox="0 0 24 24" fill="none"> <path d="M3 6h18M3 12h18M3 18h18" stroke="#111" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round" /> </svg> </div> </div> <div className="pr-2"> <svg width="20" height="20" viewBox="0 0 24 24" fill="none"> <path d="M3 3h18v18H3z" stroke="#111" strokeWidth="1.2" strokeLinejoin="round" strokeLinecap="round" /> </svg> </div> </div>

{/* Search */}
      <div className="px-4 py-3">
        <input
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          placeholder="Wallpaper"
          className="w-full rounded-xl bg-gray-100 px-4 py-3 text-sm outline-none"
        />
      </div>

      {/* Categories */}
      <div className="px-4 pb-2">
        <div className="flex gap-4 overflow-x-auto text-sm">
          {["Beauty", "Womenswear", "Electronics", "Modest wear"].map(
            (cat) => (
              <button
                key={cat}
                onClick={() => setActiveTab(cat)}
                className={`pb-2 ${activeTab === cat ? "border-b-2 border-black font-semibold" : "text-gray-400"}`}
              >
                {cat}
              </button>
            )
          )}
        </div>
      </div>
    </div>

    {/* Product Grid */}
    <div className="p-3 overflow-auto h-[calc(100%-180px)]">
      <div className="grid grid-cols-2 gap-3">
        {filtered.map((p) => (
          <div key={p.id} className="bg-white rounded-lg shadow-sm">
            <div className="aspect-[3/4] rounded-lg overflow-hidden cursor-pointer" onClick={() => setSelected(p)}>
              <img src={p.img} alt={p.title} className="w-full h-full object-cover" />
            </div>
            <div className="p-3 text-sm">
              <div className="truncate font-medium">{p.title}</div>
              <div className="mt-1 flex items-end gap-2">
                <div className="text-lg font-bold">₱{p.price.toFixed(2)}</div>
                <div className="text-xs text-gray-400 line-through">₱{p.msrp.toFixed(2)}</div>
                <div className="text-xs text-pink-500">-55%</div>
              </div>
              <div className="mt-2 text-xs text-gray-500 flex items-center gap-2">
                <div className="bg-green-100 px-2 py-1 rounded text-xs">🚚 Free ship</div>
                <div className="px-2 py-1 rounded bg-pink-50 text-pink-600">{p.tag}</div>
              </div>
            </div>
          </div>
        ))}

        {/* If there are fewer items, fill to mimic layout */}
        {filtered.length === 0 && (
          <div className="col-span-2 text-center py-10 text-gray-400">No products found</div>
        )}
      </div>
    </div>

    {/* Bottom nav like TikTok */}
    <div className="border-t px-4 py-3 flex items-center justify-between">
      <div className="flex flex-col items-center text-xs">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
          <path d="M3 3h18v18H3z" stroke="#111" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div>Home</div>
      </div>
      <div className="flex flex-col items-center text-xs">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
          <circle cx="12" cy="12" r="9" stroke="#111" strokeWidth="1.2" />
        </svg>
        <div>Friends</div>
      </div>
      <div className="rounded-full bg-black text-white w-12 h-12 flex items-center justify-center -mt-6 shadow-lg">+</div>
      <div className="flex flex-col items-center text-xs">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
          <path d="M3 12h18" stroke="#111" strokeWidth="1.2" strokeLinecap="round" />
        </svg>
        <div>Inbox</div>
      </div>
      <div className="flex flex-col items-center text-xs">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
          <path d="M12 12c2.5 0 4-1.5 4-4s-1.5-4-4-4-4 1.5-4 4 1.5 4 4 4zM3 21c0-3 4-5 9-5s9 2 9 5" stroke="#111" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div>Me</div>
      </div>
    </div>
  </div>

  {/* Product modal */}
  {selected && (
    <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50" onClick={() => setSelected(null)}>
      <div className="w-[360px] bg-white rounded-xl overflow-hidden" onClick={(e) => e.stopPropagation()}>
        <div className="p-4">
          <div className="flex items-center justify-between">
            <div className="text-lg font-semibold">{selected.title}</div>
            <div className="text-sm text-gray-500">Seller: <span className="font-semibold">hooria</span></div>
          </div>
        </div>
        <div className="h-[320px] overflow-hidden">
          <img src={selected.img} alt={selected.title} className="w-full h-full object-cover" />
        </div>
        <div className="p-4">
          <div className="text-2xl font-bold">₱{selected.price.toFixed(2)}</div>
          <div className="text-sm text-gray-400 line-through">₱{selected.msrp.toFixed(2)}</div>
          <div className="mt-3 text-sm text-gray-600">Funny description for pranking your friend Hiran. This is a fake listing for entertainment purposes. Seller: hooria.</div>
          <div className="mt-4 flex gap-2">
            <button className="flex-1 bg-black text-white py-3 rounded-lg">Buy Now</button>
            <button className="flex-1 border border-gray-200 py-3 rounded-lg">Add to Cart</button>
          </div>
        </div>
      </div>
    </div>
  )}
</div>

); }

index.html
style.css
script.js